# Exemple de variables d'environnement pour EmailJS (Vite)
# Remplacez les valeurs par celles fournies par EmailJS
# Ne commitez jamais vos vraies clés dans le dépôt public.

VITE_EMAILJS_SERVICE_ID=sgi services
VITE_EMAILJS_TEMPLATE_ID=template_jzd99uh
VITE_EMAILJS_PUBLIC_KEY=Xh9txSSI_AcwnbOPm

# Exemple de commande pour démarrer en local (PowerShell):
# $env:VITE_EMAILJS_SERVICE_ID='service_xxx'; $env:VITE_EMAILJS_TEMPLATE_ID='template_xxx'; $env:VITE_EMAILJS_PUBLIC_KEY='public_xxx'; npm run dev

# NOTE: Configurez votre template EmailJS pour envoyer à `sgiservices2015@gmail.com`.
# Dans EmailJS, créez un template contenant les variables suivantes utilisées
# dans `frontend/src/pages/Contact.jsx` :
# - title, name, email, phone, message
